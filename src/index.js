import React from 'react';
import ReactDOM from 'react-dom';
import 'antd/dist/antd.css'; // xử dụng css antd cho toàn bộ ứng dụng
import App from './luyentap/movies/app'

ReactDOM.render(
  <>
    <App />
  </>,
  document.getElementById('root')
);

